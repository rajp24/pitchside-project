# design/

Drop Claude Design canvas exports here (e.g. a `.zip` from claude.ai/design, or
the raw `.dc.html` + assets). Zips are gitignored — pull out anything that
should actually ship (copy, imagery, layout reference) into `src/` and
`public/` by hand; this folder is scratch space, not part of the build.
