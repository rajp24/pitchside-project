Drop product photos here (e.g. `hinged-front.jpg`, `mantel-433.jpg`). Once
they're uploaded, tell Claude the filenames and it'll update the `SHOTS`
array in `src/main.js` to reference them instead of the placeholder
graphics currently used for the gallery, hero, and admin thumbnails.
